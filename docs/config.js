// Fill these in once with your real GitHub repo (the same one configured
// in the app's package.json under "build.publish"). Every download button
// and version number on the site is then fetched live from GitHub's
// releases API - when you ship a new version with `npm run publish`, this
// site picks it up automatically. No need to touch this file again after
// the initial setup, and no need to edit index.html when you release
// updates.
const SITE_CONFIG = {
  GITHUB_OWNER: 'Esor48',
  GITHUB_REPO: 'easy-line-up',
};
